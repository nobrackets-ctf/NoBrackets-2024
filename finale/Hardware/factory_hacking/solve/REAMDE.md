# Solution

Le programme [solve.py](./solve.py) permet de résoudre le challenge (en ayant renseigné l'adresse IP du challenge). Il fait sortir tous les colis en exécutant ses instructions plusieurs fois pour être sûr d'être exécuté malgré les consignes du PLC.