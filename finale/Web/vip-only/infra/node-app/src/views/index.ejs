<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VIP Only</title>
    <link rel="stylesheet" href="/css/bulma.min.css">
    <style>
        .hero {
            margin-bottom: 1.5rem;
        }
        .buttons {
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <section class="hero is-primary">
        <div class="hero-body">
            <div class="container">
                <h1 class="title">Bienvenue dans VIP Only</h1>
                <h2 class="subtitle">Accédez à un contenu exclusif !</h2>
            </div>
        </div>
    </section>

    <div class="container">
        <h3 class="title">Connexion</h3>
        <form action="/login" method="POST">
            <div class="field">
                <label class="label">Nom d'utilisateur</label>
                <div class="control">
                    <input class="input" type="text" name="username" placeholder="Entrez votre nom d'utilisateur" required>
                </div>
            </div>

            <div class="field">
                <label class="label">Mot de passe</label>
                <div class="control">
                    <input class="input" type="password" name="password" placeholder="Entrez votre mot de passe" required>
                </div>
            </div>

            <div class="field buttons">
                <div class="control">
                    <button class="button is-link" type="submit">Connexion</button>
                </div>
            </div>
        </form>

        <hr>

        <h3 class="title">Inscription</h3>
        <form action="/register" method="POST">
            <div class="field">
                <label class="label">Nom d'utilisateur</label>
                <div class="control">
                    <input class="input" type="text" name="username" placeholder="Choisissez un nom d'utilisateur" required>
                </div>
            </div>

            <div class="field">
                <label class="label">Mot de passe</label>
                <div class="control">
                    <input class="input" type="password" name="password" placeholder="Choisissez un mot de passe" required>
                </div>
            </div>

            <div class="field buttons">
                <div class="control">
                    <button class="button is-link" type="submit">S'inscrire</button>
                </div>
            </div>
        </form>

        <footer class="footer">
            <div class="content has-text-centered">
                <p>Made with ❤️  by Drahoxx</p>
            </div>
        </footer>
    </div>
</body>
</html>

